import { GenericSearchResultsPipe } from './generic-search-results.pipe';

describe('GenericSearchResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericSearchResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
